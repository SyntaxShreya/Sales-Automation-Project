This is an end-to-end pipeline that:
1.  Takes **Raw CSV sales data** as input.
2.  Runs an **Analytics Engine** to calculate revenue and top products.
3.  Uses a **Reporting Engine** to generate landscape-oriented PDF reports.
4.  Automatically **archives** the results into a timestamped ZIP file for distribution.
